import 'package:flutter/material.dart';

class AlumniListScreen extends StatelessWidget {
  const AlumniListScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Alumni Directory'),
      ),
      body: const Center(
        child: Text('Alumni List Screen - Coming Soon'),
      ),
    );
  }
}
